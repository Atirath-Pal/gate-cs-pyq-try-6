{
  "id": 24,
  "year": 2024,
  "set": 2,
  "topics": [
    "Operating System",
    "Memory management"
  ],
  "type": "MSQ",
  "correct marks": 1,
  "negative marks": "0",
  "questionText": "Which of the following tasks is/are the responsibility/responsibilities of the memory management unit (MMU) in a system with paging-based memory management?",
  "assetImage": null,
  "codeBlock": null,
  "options": [
    "Allocate a new page table for a newly created process",
    "Translate a virtual address to a physical address using the page table",
    "Raise a trap when a virtual address is not found in the page table",
    "Raise a trap when a process tries to write to a page marked with read-only permission in the page table"
  ],
  "answer": [
    "B",
    "C",
    "D"
  ],
  "explanation": "<strong>Option (A) is incorrect:</strong> Allocating memory structures such as page tables for a newly created process is a software task performed by the Operating System (OS) kernel, not by the hardware Memory Management Unit (MMU).<br><br><strong>Option (B) is correct:</strong> The primary hardware role of the MMU is to perform fast translation from virtual addresses generated by the CPU into physical addresses using the page table.<br><br><strong>Option (C) is correct:</strong> If a virtual address is not present in main memory or its page table entry is marked invalid, the MMU generates a hardware trap (page fault) to transfer control to the OS page fault handler.<br><br><strong>Option (D) is correct:</strong> The MMU checks access permissions stored in the page table entries (such as read/write flags). If a process attempts to write to a read-only page, the MMU raises a protection violation trap.<br><br>Therefore, the correct answers are <strong>B, C, and D</strong>.",
  "videoUrl": null
}