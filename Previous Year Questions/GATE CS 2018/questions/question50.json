{
  "id": 50,
  "year": 2018,
  "set": null,
  "topics": [
    "Operating System",
    "Process synchronization"
  ],
  "type": "MCQ",
  "correct marks": 2,
  "negative marks": "0.66",
  "questionText": "Consider the following solution to the producer-consumer synchronization problem. The shared buffer size is $N$. Three semaphores $empty$, $full$ and $\\text{mutex}$ are defined with respective initial values of $0$, $N$ and $1$. Semaphore $empty$ denotes the number of available slots in the buffer, for the consumer to read from. Semaphore $full$ denotes the number of available slots in the buffer, for the producer to write to. The placeholder variables, denoted by $\\text{P}$, $\\text{Q}$, $\\text{R}$, and $\\text{S}$, in the code below can be assigned either $empty$ or $full$. The valid semaphore operations are: $\\text{wait}()$ and $\\text{signal}()$.<br><br><div style=\"overflow-x: auto; margin: 12px 0;\"><table style=\"border-collapse: collapse; border: 1px solid #ffffff; width: 100%; font-size: 13px;\"><thead><tr><th style=\"border: 1px solid #ffffff; padding: 8px 12px; text-align: left; font-weight: bold; width: 50%;\">Producer:</th><th style=\"border: 1px solid #ffffff; padding: 8px 12px; text-align: left; font-weight: bold; width: 50%;\">Consumer:</th></tr></thead><tbody><tr><td style=\"border: 1px solid #ffffff; padding: 12px; font-family: monospace; white-space: pre; vertical-align: top;\">do {\n    wait(P);\n    wait(mutex);\n    //Add item to buffer\n    signal(mutex);\n    signal(Q);\n} while(1);</td><td style=\"border: 1px solid #ffffff; padding: 12px; font-family: monospace; white-space: pre; vertical-align: top;\">do {\n    wait(R);\n    wait(mutex);\n    //Consume item from buffer\n    signal(mutex);\n    signal(S);\n} while(1);</td></tr></tbody></table></div><br>Which one of the following assignments to $\\text{P}$, $\\text{Q}$, $\\text{R}$ and $\\text{S}$ will yield the correct solution?",
  "assetImage": null,
  "codeBlock": null,
  "options": [
    "P: full, Q: full, R: empty, S: empty",
    "P: empty, Q: empty, R: full, S: full",
    "P: full, Q: empty, R: empty, S: full",
    "P: empty, Q: full, R: full, S: empty"
  ],
  "answer": [
    "C"
  ],
  "explanation": "<strong>Step 1: Analyze given semaphore definitions.</strong><br>• $empty$: Represents available items for the consumer to read. Initial value = $0$.<br>• $full$: Represents available spaces for the producer to write. Initial value = $N$.<br>• $\\text{mutex}$: Binary semaphore for mutual exclusion. Initial value = $1$.<br><br><strong>Step 2: Determine Producer Placeholders (P & Q).</strong><br>• Before writing an item, the producer must check if space is available to write. Thus, it must wait on $full$. Hence, $\\text{P} = full$.<br>• After writing an item, the producer increments the count of items available for the consumer to read. Thus, it signals $empty$. Hence, $\\text{Q} = empty$.<br><br><strong>Step 3: Determine Consumer Placeholders (R & S).</strong><br>• Before reading an item, the consumer must check if an item is available to read. Thus, it must wait on $empty$. Hence, $\\text{R} = empty$.<br>• After consuming an item, the consumer increments the count of free slots available for the producer. Thus, it signals $full$. Hence, $\\text{S} = full$.<br><br><strong>Conclusion:</strong><br>The correct assignment is $\\text{P}: full, \\text{Q}: empty, \\text{R}: empty, \\text{S}: full$.<br>Therefore, the correct option is <strong>(C)</strong>.",
  "videoUrl": null
}