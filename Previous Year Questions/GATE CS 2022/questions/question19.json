{
  "id": 19,
  "year": 2022,
  "set": null,
  "topics": [
    "Operating System",
    "Process synchronisation"
  ],
  "type": "MCQ",
  "correct marks": 1,
  "negative marks": "0.33",
  "questionText": "Consider the following threads, $T_1$, $T_2$, and $T_3$ executing on a single processor, synchronized using three binary semaphore variables, $S_1$, $S_2$, and $S_3$, operated upon using standard `wait()` and `signal()`. The threads can be context switched in any order and at any time.<br><br><table style=\"width: 100%; border-collapse: collapse; border: 1px solid #ffffff; margin: 12px 0;\"><thead><tr style=\"border-bottom: 1px solid #ffffff;\"><th style=\"border: 1px solid #ffffff; padding: 8px; text-align: center;\">$T_1$</th><th style=\"border: 1px solid #ffffff; padding: 8px; text-align: center;\">$T_2$</th><th style=\"border: 1px solid #ffffff; padding: 8px; text-align: center;\">$T_3$</th></tr></thead><tbody><tr><td style=\"border: 1px solid #ffffff; padding: 12px; font-family: monospace; line-height: 1.6; vertical-align: top;\">while(true) {<br>&nbsp;&nbsp;&nbsp;&nbsp;wait(S3);<br>&nbsp;&nbsp;&nbsp;&nbsp;print(\"C\");<br>&nbsp;&nbsp;&nbsp;&nbsp;signal(S2); }</td><td style=\"border: 1px solid #ffffff; padding: 12px; font-family: monospace; line-height: 1.6; vertical-align: top;\">while(true) {<br>&nbsp;&nbsp;&nbsp;&nbsp;wait(S1);<br>&nbsp;&nbsp;&nbsp;&nbsp;print(\"B\");<br>&nbsp;&nbsp;&nbsp;&nbsp;signal(S3); }</td><td style=\"border: 1px solid #ffffff; padding: 12px; font-family: monospace; line-height: 1.6; vertical-align: top;\">while(true) {<br>&nbsp;&nbsp;&nbsp;&nbsp;wait(S2);<br>&nbsp;&nbsp;&nbsp;&nbsp;print(\"A\");<br>&nbsp;&nbsp;&nbsp;&nbsp;signal(S1); }</td></tr></tbody></table><br>Which initialization of the semaphores would print the sequence $\\text{BCABCABCA....}$?",
  "assetImage": null,
  "codeBlock": null,
  "options": [
    "$S_1 = 1; S_2 = 1; S_3 = 1$",
    "$S_1 = 1; S_2 = 1; S_3 = 0$",
    "$S_1 = 1; S_2 = 0; S_3 = 0$",
    "$S_1 = 0; S_2 = 1; S_3 = 1$"
  ],
  "answer": [
    "C"
  ],
  "explanation": "<strong>Step 1: Analyze the required output sequence.</strong><br> The target sequence is $\\text{BCABCABCA....}$. The execution order of prints must be $T_2 \\rightarrow T_1 \\rightarrow T_3 \\rightarrow T_2 \\dots$<br><br><strong>Step 2: Trace thread requirements for the first cycle.</strong><br>1. <strong>Printing 'B' first:</strong> Thread $T_2$ must execute `print(\"B\")` first. It performs `wait(S1)` before printing. Hence, $S_1$ must initially be $1$.<br>2. <strong>Blocking 'C' and 'A' initially:</strong><br>- Thread $T_1$ performs `wait(S3)` before `print(\"C\")`. To ensure $T_1$ does not execute before $T_2$, $S_3$ must be $0$.<br>- Thread $T_3$ performs `wait(S2)` before `print(\"A\")`. To ensure $T_3$ does not execute before $T_2$ and $T_1$, $S_2$ must be $0$.<br><br><strong>Step 3: Verify the execution loop.</strong><br>- <strong>Start ($S_1=1, S_2=0, S_3=0$):</strong> $T_2$ executes `wait(S1)` (decrementing $S_1$ to $0$), prints 'B', and runs `signal(S3)` (setting $S_3$ to $1$).<br>- <strong>Next:</strong> $T_1$ unblocks on `wait(S3)` (decrementing $S_3$ to $0$), prints 'C', and runs `signal(S2)` (setting $S_2$ to $1$).<br>- <strong>Next:</strong> $T_3$ unblocks on `wait(S2)` (decrementing $S_2$ to $0$), prints 'A', and runs `signal(S1)` (setting $S_1$ to $1$).<br><br>This creates a repeating sequence of $\\text{BCABCABCA....}$.<br><br>Therefore, the initial semaphore values must be $S_1 = 1, S_2 = 0, S_3 = 0$, which corresponds to <strong>Option (C)</strong>.",
  "videoUrl": null
}